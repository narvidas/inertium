import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: { paddingTop: 80, paddingBottom: 30 },
  icon: {
    fontSize: 20,
    height: 22,
    color: "white",
  },
});
