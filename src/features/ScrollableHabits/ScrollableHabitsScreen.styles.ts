import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  list: {
    flex: 1,
    paddingTop: 20,
    paddingHorizontal: 20,
  },
  footerContainer: {
    paddingBottom: 200,
    alignItems: "center",
  },
});
